# YouTube toolbox

Use `api/toolbox.py` for uploads, metadata, comments, analytics, and channel operations. `browser/toolbox.py` is supervised local-only. The social media worker remains the preferred path for durable video publishing jobs.
