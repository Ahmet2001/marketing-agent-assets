# API mode

Use the official TikTok API functions listed in `manifest.yaml` in production. Keep `TIKTOK_ACCESS_TOKEN` in your secret manager and obtain the required OAuth scopes through TikTok's approval and authorization flow.
