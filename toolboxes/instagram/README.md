# Instagram toolbox

Use `api/toolbox.py` for official Graph API publishing, comments, and insights. `browser/toolbox.py` requires an interactive signed-in Selenium session and is local-only.

The exact account type, app review, and permissions required by an API action are listed in `manifest.yaml` and must be confirmed against current Meta documentation before production use.
